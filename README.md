# api-request-config

```shell
 git init
 git branch -m main

 git config user.name manhbv.giaminh
 git config user.email manhbv.giaminh@gmail.com
 git add .
 git commit -m 'api-request-config'

 git remote add origin git@github.com:ecsaas/api-request-config.git
 git push origin main
```
